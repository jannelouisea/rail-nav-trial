import React from 'react';
import { Text, StyleSheet } from 'react-native';

const BillsScreen = () => {
    return (
        <Text>This is the Bills screen</Text>
    );
};

const styles = StyleSheet.create({});

export default BillsScreen;